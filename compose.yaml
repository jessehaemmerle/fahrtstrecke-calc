services:
  fahrtstrecke:
    build: .
    ports:
      - "8080:80"
    environment:
      - STADIA_API_KEY=${STADIA_API_KEY}
    restart: unless-stopped

  # Optionaler lokaler Valhalla-Container (nur nötig wenn Stadia Maps nicht ausreicht)
  # valhalla:
  #   image: ghcr.io/gis-ops/docker-valhalla/valhalla:latest
  #   volumes:
  #     - ./valhalla_tiles:/custom_files
  #   environment:
  #     - use_tiles_ignore_pbf=${USE_TILES_IGNORE_PBF:-True}
  #     - build_tar=${BUILD_TAR:-False}
  #     - tile_urls=${VALHALLA_TILE_URL:-}
  #   healthcheck:
  #     test: ["CMD", "curl", "-sf", "http://localhost:8002/status"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 5
  #     start_period: ${VALHALLA_START_PERIOD:-120s}
  #   restart: unless-stopped
